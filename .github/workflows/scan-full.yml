name: Scan Full (Nightly)

on:
  schedule:
    - cron: '0 6 * * *' # daily at 06:00 UTC
  workflow_dispatch:

jobs:
  scan-full:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install deps
        run: npm ci
      - name: Run full test suite
        run: npm run test:repos
      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scan-full-artifacts
          path: |
            tests/test-repositories.yml

